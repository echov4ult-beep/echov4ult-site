import { describe,expect,it } from 'vitest';import { detectSlop,scoreCreative,scoreOpportunity,selectConcepts } from '../src/scoring.js';
describe('opportunity scoring',()=>{it('returns bounded total and visible dimensions',()=>{const values={visualTransformation:8,tiktokDemoPotential:9,problemClarity:8,price:7,impulsePotential:6,commissionPercent:5,expectedCommission:5,brandRecognition:7,competition:4,saturation:3,audienceSize:8,creativeAssets:7,realFootage:6,claimComplianceRisk:2,angleCount:8,adjacentExpansion:7,conversionFriction:4};const result=scoreOpportunity(values);expect(result.total).toBeGreaterThan(60);expect(Object.keys(result.contributions)).toHaveLength(17);});it('clamps invalid dimension input',()=>{const input=Object.fromEntries(['visualTransformation','tiktokDemoPotential','problemClarity','price','impulsePotential','commissionPercent','expectedCommission','brandRecognition','competition','saturation','audienceSize','creativeAssets','realFootage','claimComplianceRisk','angleCount','adjacentExpansion','conversionFriction'].map(k=>[k,20])) as any;expect(scoreOpportunity(input).total).toBeLessThanOrEqual(100);});});
describe('creative scoring',()=>{it('rewards quality and applies penalties',()=>{const high={firstFrame:9,hook:9,clarity:9,curiosity:8,movement:8,productVisibility:9,nativeFeel:8,retentionPotential:9,payoff:8,credibility:9,cta:7,novelty:8,adLikePenalty:1,repetitionPenalty:0,claimRisk:0,compliance:10,similarityPenalty:0};expect(scoreCreative(high)).toBeGreaterThan(75);expect(scoreCreative({...high,adLikePenalty:10,claimRisk:10,similarityPenalty:10})).toBeLessThan(scoreCreative(high));});it('detects AI slop',()=>expect(detectSlop({caption:'You NEED this 🔥🔥🔥🔥',overlayText:[],script:'Buy this must-have game changer.',factualClaims:[],monetized:false,requiresBurnedInDisclosure:false})).toEqual(expect.arrayContaining(['you-need-this','must-have','emoji-spam','sales-language-first-second'])));});
describe('experiment selection',()=>{it('is bounded and prioritizes information value',()=>{const factors=Array.from({length:20},(_,i)=>({productId:1,hook:`h${i}`,angle:'a',format:'f',cta:'c',priorSamples:i,uncertainty:20-i,qualityPrior:5}));const selected=selectConcepts(factors,10,3);expect(selected).toHaveLength(3);expect(selected[0]!.hook).toBe('h0');});});
